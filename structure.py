# how to structure python modules
# using functions, loops, conditionals, __main__
def myFn(): # the colon indicates the start of a code block
    return 'hello'    # every indented line belongs to this code block

# when the lines are no longer indented, the code block has ended

result = myFn() # we invoke the function
print(result)
